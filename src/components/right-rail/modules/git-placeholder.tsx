export function GitPlaceholderModule() {
  return (
    <div className="rounded-md border border-dashed border-sidebar-border bg-sidebar-accent/40 px-3 py-6 text-center">
      <p className="text-sm font-medium text-sidebar-foreground">Git</p>
      <p className="mt-1 text-xs text-muted-foreground">占位模块：后续可接变更 / 提交 / 分支。</p>
    </div>
  );
}
